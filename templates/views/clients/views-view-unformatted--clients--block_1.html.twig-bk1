<div class="home1 home1-carousel">
    <div class="container">
        <div class="home1-carousel-block">
            <div class="carousels-block">
                <div class="carousels5">
                    <div class="owl-carousel owl-theme" id="owl-carousel2">
                        {% for row in rows %}
                            {{ row.content }}
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>